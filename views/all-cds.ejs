<!DOCTYPE html>
<html>
  <head>
    <title>All CDs</title>
    <link rel="stylesheet" href="/style.css">
  </head>
  <body>
<!-- Representing a list of CDs -->
 <div class="control" itemscope itemtype="http://schema.org/MusicAlbum"> 
      <h2>CD Collection</h2>
      <% if (items.length > 0) { %>
    <ul>
      <% items.forEach(function(item) { %>
     <li><a href="/cds/<%=item._id%>">
     <span itemprop="name"><%=item.name%></span></a></li>
      <% }); %>
    </ul>
    <% } else { %>
    <div>No cds have been added to the collection.</div>
    <% } %>
    <h2>Don't see a CD you own? Add one!</h2> 
      <script>
      function onFormSubmit(form) {
        form.action = form.action + form.elements['item[id]'].value;
        console.log(form.action);
        return true;
      } 
      </script>
      <form method="post" action="/cds/"
            onsubmit="return onFormSubmit(this);">
        <!-- The line above dynamically sets the PUT destination when the -->
        <!-- form is submitted. -------------------------------------------->
        <input type="hidden" name="_method" value="put">
        <!-- The line above tells the server to treat this as a PUT request. -->
        <p><label>CD-ASIN:
          (lowercase letters only, no spaces):
          <input name="item[id]" type="text" required
                 placeholder="B00000DFTD">
        </label></p>
        
        <p><label>CD name: 
          <input name="item[name]" type="text" required
                 placeholder="FanMail">
        </label></p>
        
        <p><label>CD Genre:
          <input name="item[genre]" type="text" required
                    placeholder="Hip Hop">
        </label></p>
        
        <p><label>CD Artist</label>
            <input name="item[artist]" type="text" required 
                    placeholder="TLC">
        </label></p>
        <button>Add or update</button>
      </form>
      </div>
      
     Go back <a href="/">home</a>?<br />
     Look at some <a href="/genres/">genres</a>?<br />
     Explore the <a href="/tracks/">track collection</a>?
    
</body>
</html> 
